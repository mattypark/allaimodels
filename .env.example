.env.example — this project needs no API keys.

# Every data source is public and keyless (lab docs, HuggingFace API,
# GitHub API, YouTube oEmbed). Nothing here is required to build or run.
# Optional: raises the GitHub API rate limit for the weekly scraper.
# GITHUB_TOKEN=
